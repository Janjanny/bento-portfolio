import React from 'react'

const ProjectCard = ({img, title, desc, githubLink, projectLink}) => {
  return (
    <div className=' h-max bg-button-color'>
        Project
    </div>
  )
}

export default ProjectCard